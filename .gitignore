# Local dev tooling / agent artifacts — not part of the site deliverable
.claude/
.agents/
skills-lock.json

# React/shadcn scaffolding unused by this vanilla HTML site
components/
lib/

# Local experiments / notes
hero-concepts/
Claude.md

# Eredeti, nagy méretű forrásképek – a webes (WebP) változatok a képek/ mappában vannak
elküldött képek/

# Admin: titkos konfiguráció (jelszó-hash) — SOHA ne kerüljön a repóba
/inc/config.php
# Futásidejű fájlok
/content/.login_throttle.json
# Feltöltött képek (felhasználói tartalom) — a mappát és védelmét megtartjuk
/uploads/*
!/uploads/.htaccess
!/uploads/index.html
